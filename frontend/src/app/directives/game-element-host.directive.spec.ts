import { GameElementHostDirective } from './game-element-host.directive';

describe('GameElementHostDirective', () => {
  it('should create an instance', () => {
    const directive = new GameElementHostDirective();
    expect(directive).toBeTruthy();
  });
});

import { GameElementHost } from './game-element-host';

describe('GameElementHost', () => {
  it('should create an instance', () => {
    const directive = new GameElementHost();
    expect(directive).toBeTruthy();
  });
});
